# Python build artifacts
__pycache__/
*.pyc

# Manuscript drafts (kept local — not part of the public corpus/tooling)
Doktorarbeit_final.md
docs/The-Logographic-Shift.md
docs/The-Logographic-Shift*.md
docs/Doktorarbeit_final.docx
docs/The-Logographic-Shift.docx
docs/The-Logographic-Shift-1.docx
# Review PDFs exported from the .md (see scripts/export_article.sh)
docs/The-Logographic-Shift*.pdf
# Word exports with the article's content (see scripts/export_docx.py) and
# their staging/delivery directories — the manuscript stays out of the repo
docs/The-Logographic-Shift*.docx
docs/_build/
docs/_delivery/

# Editor backup files
*.bak
*.bak[0-9]*
*.bak[A-Za-z]*

# CHR 2027 submission — double-blind review, so the draft must not be findable
# in a public, eponymous repository. Generated sweep output is regenerable via
# scripts/restoration_sweep.py.
docs/chr2027-*
docs/restoration-sweep.csv

# Word exports of the source catalogue (regenerated from the .md via pandoc)
docs/catalogue-of-sources*.docx

# Office / editor lock and temp files
~$*
.~lock.*#
~WRL*.tmp

# Local logs and Claude Code state
*.log
.claude/

# KAL 5 tooling — one-off ingestion/scan scripts that hard-code a local PDF path
scripts/*kal5*.py

# Texts imported through the app's Upload dialog — the reader's own corpus,
# kept beside the published one on disk but never part of it (see CUSTOM_DIR).
data/_custom/

# Copyrighted third-party material
docs/Rochberg-1988-AfO-Beih-22-pp1-31.md

# Working notes
TODO.md

# Scratch / debugging scripts and fixtures
debug_loading.py
reproduce_complex.py
reproduce_issue.py
test_parsing.py
complex_input.txt
ebl_ms3117.txt

# Desktop (PyInstaller) build — see packaging/desktop_exe.spec
packaging/build/
packaging/dist/

# Desktop (stlite/Electron) build — heavy artifacts, generated locally
desktop/node_modules/
desktop/build/
desktop/dist/
desktop/package-lock.json
# staged app files (copied from repo root by prepare-app.js)
desktop/streamlit_app.py
desktop/references.bib
desktop/data/
desktop/assets/
desktop/docs/

# Generated static web build (stlite/browser) — deployed by CI
desktop/web/

# Reference PDFs kept locally, not distributed
assets/literature/

# extracted reviewer comments (names + unpublished feedback)
docs/reviewer-comments.md
docs/*.prev

# working research notes (unpublished agenda)
docs/civil-encoding-agenda.md
