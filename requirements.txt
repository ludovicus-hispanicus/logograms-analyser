# Pinned for the archived release: the versions v1.0.0 was developed and tested
# against, so the Zenodo copy still runs years from now. Streamlit in particular
# moves fast and has already scheduled the removal of APIs this app uses
# (use_container_width, st.components.v1.html), so an unpinned install would
# break the app rather than merely change it.
#
# To follow upstream instead, relax these to >= and expect to fix deprecations.
streamlit==1.58.0
pandas==3.0.0
plotly==6.8.0
PyYAML==6.0.3
streamlit-ace==0.1.1

# Desktop window for desktop.py (real CPython → eBL fetch + disk persistence).
# Not needed to run the web app; only for the packaged desktop build.
pywebview==6.2.1

# Developed on CPython 3.12 (3.12.9); the packaged desktop build is frozen with
# PyInstaller 6.22.2 (see packaging/desktop_exe.spec).
